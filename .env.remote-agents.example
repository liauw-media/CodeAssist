# Remote Code Agents Configuration Example
# Copy this to .env and fill in your actual values
# IMPORTANT: Never commit the .env file with real credentials!

# API endpoint for your remote Claude Code agents
# Examples:
#   - Local network: http://192.168.1.100:8080
#   - Tailscale VPN: http://agent-host:8080
#   - WireGuard VPN: http://10.0.0.50:8080
#   - Localhost (if running locally): http://localhost:8080
export REMOTE_AGENT_API_URL="http://your-agent-host:8080"

# Secure API key for authentication
# Generate a secure key with: openssl rand -hex 32
# This should match the API_KEY set in your agent's .env file
export REMOTE_AGENT_API_KEY="your-secure-api-key-here"

# Default agent type to use when not specified
# Options: general, research, testing
export REMOTE_AGENT_DEFAULT_TYPE="general"

# Optional: Timeout for waiting on tasks (in seconds)
export REMOTE_AGENT_TIMEOUT=3600

# Optional: Enable verbose logging
export REMOTE_AGENT_DEBUG=false
